import { FKSpec, TableData } from ".";
import { AutoOptions, CaseOption, LangOption } from "./types";
export declare class AutoWriter {
    tableText: {
        [name: string]: string;
    };
    foreignKeys: {
        [tableName: string]: {
            [fieldName: string]: FKSpec;
        };
    };
    options: {
        caseFile?: CaseOption;
        caseModel?: CaseOption;
        caseProp?: CaseOption;
        directory: string;
        lang?: LangOption;
        noWrite?: boolean;
    };
    constructor(tableData: TableData, options: AutoOptions);
    write(): Promise<void> | Promise<void[]>;
    private createFile;
    /** Create the belongsTo/hasMany/hasOne association strings */
    private createAssociations;
    private createTsInitString;
    private createES5InitString;
}
